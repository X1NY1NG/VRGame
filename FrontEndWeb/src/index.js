import React from "react";
import ReactDOM from "react-dom/client";
import PersonalizationUploader from "./pages/PersonalizationUploader";
import "./style.css"; // add this line for the improved CSS

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(<PersonalizationUploader />);
